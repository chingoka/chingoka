export interface Product {
    id?: number;
    name: string;
    category: number; // Category ID
    quantity: number;
    size: number; // Size ID
    store: string;
    user: number; // User ID
  }
  